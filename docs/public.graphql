# Entry points

type Query {

  # Check system status [GET /]
  status: Status
}

type Mutation {

  # Create new project and execute
  run(name: ID!, config: JSON!): Boolean

  # Set concurrent evals
  setConcurrent(name: ID!, concurrent: Int!): Boolean
}

# Inputs

# Objects

type Status {
  version: String
  commitHash: String
}

# Scalars

scalar JSON
